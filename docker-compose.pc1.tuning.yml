services:
  janus-api:
    environment:
      - QDRANT_HTTPS=${QDRANT_HTTPS:-false}
      - LLM_EXECUTOR_MAX_WORKERS=${LLM_EXECUTOR_MAX_WORKERS:-8}
      - TOOL_EXECUTOR_MAX_CONCURRENCY=${TOOL_EXECUTOR_MAX_CONCURRENCY:-4}
      - ENABLE_GOOGLE_PRODUCTIVITY_WORKER=${ENABLE_GOOGLE_PRODUCTIVITY_WORKER:-false}
