# Janus Backend

This directory contains the FastAPI backend for Janus.

**Please refer to the [root README](../README.md) for full documentation and setup instructions.**

## Quick Start

```bash
pip install -r requirements.txt
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```
